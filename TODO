[ ] Add mermaid output
[ ] Handle the inherit type from parent
[ ] Private or public variables (+ & -)
[ ] Do not handle a direct alocation of a variables from init. For exemple here , the Type of employee_id and salary is Name

class Employee(Person):
    def __init__(self, name: str, age: int, employee_id: int, salary: float):
        super().__init__(name, age)
        self.employee_id = employee_id
        self.salary = salary
